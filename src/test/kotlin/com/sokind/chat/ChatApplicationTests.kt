package com.sokind.chat

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

@Disabled("PersistenceIT(Testcontainers MySQL) + TimelineServiceUnitTest(mockk) 로 대체됨")
class ChatApplicationTests {
    @Test fun `자리표시자`() = Unit
}
