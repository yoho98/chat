# Software Engineer(Backend) 사전 과제
## 실시간 통신 + 이벤트 기반 상태 복원 기능

> **출처**: https://sokind.notion.site/Software-Engineer-Backend-32603c20b84280dfa299c2a23e14547c
>
> **확인 메일 필수**: 과제 내용 확인 후 `miyeon@creatived.kr`로 "확인했다"는 간단한 메일을 보내야 함 (리멤버 알림 못 보시는 분이 있어서 확인 차원)

---

## 목적
- **동작하는 구현 / 설계 역량(정합성·확장성·장애대응·관측가능성) / 백엔드 기본기(DB·쿼리·REST)** 검증
- 모든 항목 완전 구현 불필요 → **미구현 항목은 설계 문서로 대체 가능**
- 평가 요소: 구현 완성도 + 설계 근거 + 트레이드오프 설명 + 문서 품질
- 추가 구현은 가산점 (단, 무엇을 추가했는지 명확히 기술)

---

## 1. 과제 시나리오
**1:1 참여자 간 실시간 채팅/통신 서비스 설계·구현**
+ 대화 중 발생한 이벤트 기반으로 **특정 시점의 대화 상태 복원** 가능해야 함 (Event Sourcing & Determinism 검증)

### 핵심 도메인
- **Session**: 시작/종료, 참여자, 상태(진행중/중단/완료)
- **Event/Message**: message, join, leave, disconnect, reconnect 등
- **Snapshot/Projection**: 특정 시점 세션 상태 복원

---

## 2. 중요 제약 (필수)

### 2.1 중복 이벤트 발생 가능
- 네트워크 불안정으로 클라이언트가 동일 이벤트 재전송 가능
- 서버는 중복 저장·중복 반영 최소화 전략 필요

### 2.2 순서 뒤바뀜 발생 가능
- 메시지/이벤트 도착 순서가 뒤바뀔 수 있음
- 일관된 정렬 기준 정의 필요 (예: 서버 수신 시각, 클라이언트 시각, 시퀀스 번호 등)

---

## 3. 구현 범위
- 백엔드 역량 평가 목적 → **FE/UI 구현 불필요**
- 검증 수단: WebSocket 테스트 클라이언트 / CLI 스크립트 / Postman / 재현 절차 문서 中 택1
- **기술 스택 자유** (단, 선택 근거 명시)

### 비목표 (Non-goals)
- FE/Web/Mobile UI 구현
- 운영 배포 자동화·인프라 완성도
- 인증/인가 체계의 완결성
- 제품 수준 UX/관리 도구

---

## 4. 요구사항

### 4.1 필수 구현 (실제 동작)
- 실시간 메시지 송수신
- 사용자 join / leave 처리
- 기본 **presence**(online/offline 또는 세션 참여 상태)
- 이벤트/메시지 수집 API
- **중복 이벤트 방지** 전략
- **순서 뒤바뀜** 일관된 처리 기준
- **특정 시점 기준 세션 상태 복원 API** 1개

### 4.2 필수 설계/문서 (구현 또는 문서 택1)

**DB 설계 제출물**
- ERD 또는 테이블 관계 설명 문서
- 핵심 DDL 일부 (Session, Participant, Event/Message, Snapshot/Projection)
- 주요 조회 인덱스 설계 근거 (핫패스 중심)
- 정규화/비정규화/JSONB 선택 자유 → **근거·트레이드오프 명시**

**REST API 설계 제출물 (OpenAPI 권장)**

| Method | Path | 설명 |
|---|---|---|
| POST | `/sessions` | 세션 생성 |
| POST | `/sessions/{id}/join` | 참여 |
| POST | `/sessions/{id}/events` | 이벤트/메시지 수집 |
| POST | `/sessions/{id}/end` | 종료 |
| GET | `/sessions` | 목록 조회 (기간/상태/참여자 필터) |
| GET | `/sessions/{id}/timeline?at=...` | 특정 시점 상태 복원 |
| (선택) POST | `/sessions/{id}/snapshots` | 스냅샷 생성/갱신 |
| (선택) GET | `/sessions/{id}/events?from=...&to=...` | 디버깅/검증용 |

**설계 문서 포함 항목**
- 재연결 시 데이터 정합성 유지 방식
- 서버 수평 확장 시 세션 분산·상태 저장 전략
- 관측 가능성 (로그/메트릭/추적)
- 비동기 처리 구조 (Projection 업데이트, Snapshot 생성, 재시도, **DLQ**, **Idempotency**)
- 장애 대응 시나리오 (서버 다운 / DB 장애 / 데이터 유실·정합성)

### 4.3 이벤트 기반 상태 복원 기능

**복원 대상 예시 (특정 시점 `t` 기준)**
- 참여자 목록 (입장/퇴장 반영)
- 메시지 목록 (또는 최근 N개)
- 메시지 상태 (전송/수정/삭제 반영)
- (선택) unread count, typing indicator

**필수 설명**
- 프로젝션/리플레이 전략
- 중복·순서 뒤바뀜을 복원 로직에서 다루는 방법
- 복원 비용·성능 고려 설계 (인덱스, 스냅샷 주기, 저장 포맷)
- 전체 리플레이 vs 스냅샷 + 리플레이 선택

**가산점**
- Snapshot + Replay 기반 복원 최적화
- Projection 비동기화
- 재연결 시 resume / replay 고도화

### 4.4 쿼리 최적화 및 트러블슈팅 (문서 제출)

**(1) 쿼리/인덱스 최적화**
- 대화 데이터가 대량으로 쌓일 때의 조회 성능 전략
- 핫패스 기준 2~3개 주요 쿼리에 대해:
  - SQL
  - 인덱스 설계
  - 예상 병목 및 개선 방향 (텍스트 설명)

**(2) 비동기 처리 설계** (해당되는 경우)
- Snapshot 생성 또는 Projection 업데이트를 비동기로 분리하는 경우의 구조
- 재시도(지수 백오프), DLQ, Idempotency, 중복 실행 방지 설계

**(3) 장애 대응 시나리오** (감지 → 완화 → 복구 흐름)
- 서버 다운(인스턴스 장애)
- DB 장애 또는 성능 저하(커넥션 고갈, 락 경합 등)
- 데이터 유실 또는 정합성 이슈(중복 저장, 부분 실패 등)

---

## 5. 가산점 항목
- Snapshot 생성 자동화
- Projection 비동기 파이프라인 구성
- 부하 테스트 및 성능 측정 결과
- 운영 대시보드 / 메트릭 시각화
- WebRTC 등 추가 통신 방식 비교 또는 구현
- 테스트 전략 고도화 (통합 테스트, 장애 주입 테스트, 재현 스크립트)

---

## 6. 제출물 체크리스트
- [ ] **README**: 실행 방법, 환경 구성, 주요 의사결정 요약
- [ ] **API 명세** (OpenAPI 권장)
- [ ] **ERD + 핵심 DDL**
- [ ] **주요 쿼리 2~3개 + 인덱스 근거 + 병목 설명**
- [ ] **설계 문서**: 재연결, 중복 처리, 확장성, 관측 가능성, 장애 대응
- [ ] **이벤트 기반 상태 복원** 설계 또는 구현 결과
- [ ] (선택) Snapshot/Projection 고도화 구현
- [ ] (선택) 부하 테스트 결과, 대시보드, 추가 통신 방식 검토

---

## 7. 참고
- 과제 진행 방식에 별도 제약 없음
- AI 활용 / 직접 구현 / 혼합 **모두 무방**
- 가정이 필요한 부분은 **가정을 명시**하고 진행
- 자유롭게 진행 가능

---

## 8. 제출 방법
- **public 저장소** 생성 후 링크를 `miyeon@creatived.kr`로 공유
- 기한보다 사전 제출 무방
